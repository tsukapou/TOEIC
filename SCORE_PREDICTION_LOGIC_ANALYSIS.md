# 学習状況の予測スコアロジック分析レポート 📊

## 調査日時
2025-12-08

## エグゼクティブサマリー
ツカサさんのTOEIC Part5学習アプリには、**3つの独立した予測スコア計算ロジック**が存在していることが判明しました。それぞれ異なるアルゴリズム、異なる目的、異なる精度を持っています。

---

## 🎯 発見された3つのスコア予測システム

### 1️⃣ **メインシステム（app.js）** - 最も精密
**ファイル**: `js/app.js`
**関数**: `predictTOEICScore(correctCount, totalQuestions)`

#### アルゴリズム
```javascript
const accuracy = (correctCount / totalQuestions) * 100;

// PART5スコア予測テーブル
if (accuracy >= 95) return 180;
if (accuracy >= 90) return 170;
if (accuracy >= 85) return 160;
if (accuracy >= 80) return 150;
if (accuracy >= 75) return 140;
if (accuracy >= 70) return 130;
if (accuracy >= 65) return 120;
if (accuracy >= 60) return 110;
if (accuracy >= 55) return 100;
if (accuracy >= 50) return 95;
if (accuracy >= 45) return 90;
if (accuracy >= 40) return 85;
return 75; // <40%
```

#### 特徴
- **精度区間**: 15段階
- **PART5スコア範囲**: 75～180点
- **総合スコア推定**: `PART5スコア × 2.75 = リーディング推定スコア`
- **スコアレベル判定**:
  - 🏆 **Aレベル（ハイスコア）**: 推定リーディング ≥450
  - 📘 **Bレベル（上級）**: 推定リーディング ≥400
  - 📙 **Cレベル（中上級）**: 推定リーディング ≥350
  - 📗 **Dレベル（中級）**: 推定リーディング ≥300
  - 📕 **Eレベル（初中級）**: 推定リーディング ≥250
  - 📔 **Fレベル（基礎）**: 推定リーディング <250

#### 使用場面
- 🎯 **テスト結果画面** (`showResultScreen`)
- 📊 **リアルタイム結果分析**
- 💾 LocalStorageへの保存

---

### 2️⃣ **パーソナライズドナビゲーションシステム（personalized-learning-nav.js）** - 目標達成予測型
**ファイル**: `js/personalized-learning-nav.js`
**関数**: `estimateCurrentScore()`

#### アルゴリズム
```javascript
// 過去5回のテスト履歴を使用
const recentTests = history.slice(-5);
const avgAccuracy = recentTests.reduce((sum, test) => sum + test.accuracy, 0) / recentTests.length;

// 推定スコア計算式
const estimatedScore = 400 + (avgAccuracy * 400);
```

#### 特徴
- **ベーススコア**: 400点（初期値）
- **最大変動幅**: ±400点
- **計算方法**: 線形補間式
  - 正答率0% → 400点
  - 正答率50% → 600点
  - 正答率100% → 800点
- **サンプル数**: 直近5回のテスト

#### 使用場面
- 📈 **目標達成までの日数予測** (`estimateDaysToGoal`)
- 🧭 **学習ナビゲーション画面**
- 📅 **学習計画の進捗管理**

#### 目標達成予測ロジック
```javascript
// 週次改善率を計算
const weeklyImprovement = (trend.current - trend.oneWeekAgo) / 7;

// 到達日数計算
if (currentScore >= targetScore) return 0; // すでに達成
if (weeklyImprovement <= 0) return 999; // 改善なし
const weeksNeeded = Math.ceil((targetScore - currentScore) / weeklyImprovement);
return weeksNeeded * 7; // 日数に変換
```

---

### 3️⃣ **秘書モチベーションシステム（secretary-motivation.js）** - 簡易版
**ファイル**: `js/secretary-motivation.js`
**関数**: `calculatePredictedScore(correctRate)`

#### アルゴリズム
```javascript
// 正答率を9段階で予測
if (correctRate >= 0.95) return 950;
if (correctRate >= 0.90) return 900;
if (correctRate >= 0.85) return 850;
if (correctRate >= 0.80) return 800;
if (correctRate >= 0.75) return 750;
if (correctRate >= 0.70) return 700;
if (correctRate >= 0.65) return 650;
if (correctRate >= 0.60) return 600;
if (correctRate >= 0.55) return 550;
return 500; // <55%
```

#### 特徴
- **精度区間**: 9段階
- **スコア範囲**: 500～950点
- **粒度**: 50点単位
- **特徴**: 非常にシンプル、ざっくり推定

#### 学習ペース判定ロジック
```javascript
if (currentStreak >= 7 && completedTests >= 10) return 'excellent';
if (currentStreak >= 3 && completedTests >= 5) return 'good';
if (completedTests >= 3) return 'moderate';
return 'beginner';
```

#### 使用場面
- 💬 **秘書からのパーソナライズドメッセージ生成** (`getHomeMessage`)
- 🎉 **モチベーション維持のための励まし**
- 📢 **ホーム画面での挨拶メッセージ**

---

## 📊 3システムの比較表

| 項目 | app.js<br>メインシステム | personalized-learning-nav.js<br>ナビゲーション | secretary-motivation.js<br>秘書モチベーション |
|------|:---:|:---:|:---:|
| **精度レベル** | ★★★★★ 最高 | ★★★★☆ 高 | ★★☆☆☆ 簡易 |
| **区間数** | 15段階 | 連続値 | 9段階 |
| **スコア範囲** | 75～180点<br>(PART5) | 400～800点<br>(総合) | 500～950点<br>(総合) |
| **計算基準** | 1回のテスト結果 | 過去5回の平均 | 全体の正答率 |
| **更新頻度** | テスト毎 | 5回毎に変化 | リアルタイム |
| **使用目的** | 公式結果表示 | 学習計画・予測 | 励まし・モチベ |

---

## 🔍 重要な発見

### ✅ **一貫性の問題**
1. **異なるスコアが表示される可能性**
   - メインシステム: 「あなたのPART5推定は160点です」
   - ナビゲーション: 「現在の推定スコアは650点です」
   - 秘書: 「予測スコアは750点ですね！」
   
   → **ユーザーが混乱する可能性あり** ⚠️

2. **計算式の精度差**
   - メインシステムは**TOEIC公式に準拠**した精密な変換テーブル
   - ナビゲーションは**簡易線形式**（400 + 正答率×400）
   - 秘書システムは**50点単位の大雑把な推定**

---

## 💡 推奨事項

### 🎯 短期改善案
1. **統一インターフェースの作成**
   ```javascript
   // 新しい統一スコア取得関数
   function getUnifiedPredictedScore() {
       // メインシステムの精密ロジックを使用
       const mainScore = predictTOEICScore(...);
       return mainScore;
   }
   ```

2. **用途別の明確化**
   - **公式結果**: app.jsのメインシステム
   - **学習計画**: personalized-learning-nav.jsの平均値システム
   - **モチベーション**: secretary-motivation.jsの簡易システム（励まし用）

3. **UIでの表示方法改善**
   ```
   [結果画面]
   PART5推定スコア: 160点 / 200点満点
   総合推定スコア: 440～500点
   
   [学習ナビ]
   現在の平均レベル: 約650点（過去5回平均）
   目標800点まで: あと約45日
   
   [秘書メッセージ]
   「いい感じですね！推定700点台です！✨」
   ```

### 🚀 長期改善案
1. **統一スコア予測エンジンの構築**
   - 1つの精密なロジックに統合
   - 用途に応じてパラメータ調整
   - 履歴データを活用した機械学習的アプローチ

2. **スコア予測の信頼度表示**
   ```
   推定スコア: 650点 ± 50点
   信頼度: ★★★☆☆（テスト回数: 5回）
   ```

3. **TOEIC公式データとの相関分析**
   - 実際のTOEICスコアとの誤差測定
   - 予測精度の継続的改善

---

## 📝 技術的詳細

### メインシステムの換算式
```javascript
// PART5 → 総合リーディングスコア
const totalReading = part5Score * 2.75;

// 総合スコア範囲計算
const minTotalScore = Math.round((totalReading * 2) * 0.95);
const maxTotalScore = Math.round((totalReading * 2) * 1.05);
```

### ナビゲーションシステムの目標達成予測
```javascript
// 1週間前のスコア取得
const oneWeekAgo = history.filter(test => {
    const testDate = new Date(test.timestamp);
    const weekAgoDate = new Date(Date.now() - 7 * 24 * 60 * 60 * 1000);
    return testDate <= weekAgoDate;
});

// 改善トレンド計算
const trend = {
    current: estimateCurrentScore(),
    oneWeekAgo: calculateOldScore(oneWeekAgo)
};

// 週次改善率
const weeklyImprovement = (trend.current - trend.oneWeekAgo) / 7;
```

---

## 🎓 結論

ツカサさんのアプリには、**それぞれ異なる目的を持つ3つのスコア予測システム**が共存しています：

1. **app.js** → 🎯 **精密な公式結果表示用**（最も信頼性が高い）
2. **personalized-learning-nav.js** → 📈 **学習計画・目標達成予測用**（トレンド重視）
3. **secretary-motivation.js** → 💪 **モチベーション維持用**（励まし重視）

### 現状の評価
✅ **良い点**:
- 各システムが目的に最適化されている
- 用途に応じた精度のトレードオフ
- 処理速度が速い（軽量な計算）

⚠️ **改善点**:
- ユーザーに複数の異なるスコアが表示される可能性
- システム間の整合性が不明確
- 統一的なスコア管理が不在

---

## 📌 次のアクションアイテム

1. ✅ **調査完了** - 3つのシステムの存在を確認
2. ⏳ **ユーザーフィードバック待ち** - どのシステムを信頼すべきか決定
3. 🔜 **統一化の検討** - 必要に応じて統合ロジック作成
4. 🔜 **ドキュメント整備** - 各システムの役割を明文化

---

**作成者**: AI Assistant  
**調査対象**: TOEIC Part5 学習アプリ  
**調査範囲**: js/app.js, js/personalized-learning-nav.js, js/secretary-motivation.js  
**調査完了日**: 2025-12-08
